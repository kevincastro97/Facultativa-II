/**
 * @format
 */

import {AppRegistry} from 'react-native';
import StartContainer from './src/containers/stratContainer';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => StartContainer);
