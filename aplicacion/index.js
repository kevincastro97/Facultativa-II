/**
 * @format
 */

import {AppRegistry} from 'react-native';
import StratContainer from './src/containers/stratContainer';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => StratContainer);
